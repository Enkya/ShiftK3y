import React from 'react';
import { withRouter } from 'react-router-dom';

/**
 * @name Home
 * @summary renders Home component
 * @extends React.component
 */
const Home = () => (<span>TBD</span>);

export default withRouter(Home);
